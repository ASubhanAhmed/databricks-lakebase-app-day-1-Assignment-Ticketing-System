# Local development only. Copy to .env and paste your own value.
# .env is gitignored - never commit a real connection string.
LAKEBASE_URL=postgresql://<role>:<password>@<host>.database.cloud.databricks.com:5432/databricks_postgres?sslmode=require

SEED_SAMPLE_DATA=true
LOCAL_USER=you@example.com
